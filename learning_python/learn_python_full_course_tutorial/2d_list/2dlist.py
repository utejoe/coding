#creating a list an making all the elements inside of the list list themselves
number_grid = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
    [0]
]

print(number_grid[0]) # accessing the list index 0 in the list
print(number_grid[3]) # accessing the index list 3 in the list
print(number_grid[2][1])# accessing an element using the row and column index
print(number_grid[2][2]) #accessing an element using the row and column indixes of the element
#note the [row][column] and it indices starts from 0