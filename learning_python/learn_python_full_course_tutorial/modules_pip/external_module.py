# importing the external modules.. 
# we use python-docx to install the module while docx to import the module
import docx
docx. # checking the availabele functions for the docx


# to unisall the module we just use 'pip unistall python-module_name'